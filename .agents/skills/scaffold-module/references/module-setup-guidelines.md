# Module Setup Guidelines

Follow these steps in order when scaffolding a new module.

## 0. Confirm dependencies

- Use only packages listed in `.agents/context/tech-stack.md`.
- If the module needs an extra package, propose it to the user first. If they decline, continue with the existing stack — do not abort.
